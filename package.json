{
  "name": "Charlie's Barkery",
  "version": "1.0.0",
  "dependencies": {
    "@tailwindcss/vite": "^4.2.1",
    "tailwindcss": "^4.2.1"
  },
  "devDependencies": {
    "vite": "^7.3.1"
  },
  "scripts": {
    "up": "ddev start && ddev npm install && ddev composer install",
    "dev": "vite"
  }
}
